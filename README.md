# PythonBasic
Python Small Basic — A Python library inspired by Microsoft Small Basic for teaching programming to beginners. Draw shapes, use Turtle graphics, handle mouse/keyboard events, create GUI controls, play sounds, make network requests, and more. Windows only. Zero external dependencies (uses only Python's standard library).
